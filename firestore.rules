rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny-all. Real rules are written once quiz data exists (see build plan).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
