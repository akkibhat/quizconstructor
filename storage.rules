rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny-all. Real rules are written once quiz media exists (see build plan).
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
